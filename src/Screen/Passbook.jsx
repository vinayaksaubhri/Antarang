import React from 'react';
import { View, Text } from 'react-native';
import { Tab_view } from '../../style/global';

const Passbook = () => {
  return (
    <Tab_view>
      <Text>PASSBOOK SCREEN</Text>
    </Tab_view>
  );
};

export default Passbook;

import React from 'react';
import { View, Text } from 'react-native';
import { Tab_view } from '../../style/global';

const Notification = () => {
  return (
    <Tab_view>
      <Text>NOTIFICATION SCREEN</Text>
    </Tab_view>
  );
};

export default Notification;
